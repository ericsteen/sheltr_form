<%= form_with(model: user_answer, local: true) do |form| %>
  <% if user_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_answer.errors.count, "error") %> prohibited this user_answer from being saved:</h2>

      <ul>
      <% user_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
