<%= form_with(model: user_questionnaire, local: true) do |form| %>
  <% if user_questionnaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_questionnaire.errors.count, "error") %> prohibited this user_questionnaire from being saved:</h2>

      <ul>
      <% user_questionnaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :questionnaire_id %>
    <%= form.text_field :questionnaire_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
